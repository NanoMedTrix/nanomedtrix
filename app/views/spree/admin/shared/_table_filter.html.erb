<% if content_for?( :table_filter ) %>
	<div id="table-filter" data-hook class="col-sm-12">
		<fieldset>
			<h4 class="sub-header"><%= yield :table_filter_title %></h4>

			<%= yield :table_filter %>
		</fieldset>
	</div>
<% end %>
