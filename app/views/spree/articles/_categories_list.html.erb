<ul class="news_categories_list">
  <% Spree::Article.visible.tag_counts_on( :categories ).sort_by( &:count ).reverse.each do | category | %>
    <li><%= link_to category.name, news_category_path( category.name ) %> (<%= category.count %>)</li>
  <% end %>
</ul>
